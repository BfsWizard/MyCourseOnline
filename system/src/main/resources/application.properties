spring.application.name=system
server.servlet.context-path=/system
server.port=9001
#增加配置，指向注册中心的地址
ureka.client.service-url.defaultZone=http://localhost:8761/eureka/

#数据库连接
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasourse.url=jdbc:mysql://localhost:3306/mall?characterEncoding=utf-8&useSSL=false&autoReconnect=true
spring.datasourse.username=root
spring.datasourse.password=abc123456

mybatis.mapper-locations=classpath:/mapper/*.xml

logging.level.com.course.system.dao=trace
